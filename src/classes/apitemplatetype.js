/**
 * Object representing a template type of a class, struct or method.
 */

export default class APITemplateType
{
	/** Defined identifier for the template type. */
	identifier = null;

	/** Constraints of the template type. */
	constraints = [];

	/** Description of the template type's use. */
	description = null;
}